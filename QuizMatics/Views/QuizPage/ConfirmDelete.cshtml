@model QuizMatics.Models.QuizDto

<div class="text-center">
    <h2 class="text-center">Are you sure you want to delete this quiz?</h2>
</div>

<div class="text-center mb-4">
    <p class="text-primary">Title: @Model.Title</p>
    <p><strong>Description:</strong> @Model.Description</p>
</div>

<div class="text-center">
    <form method="post" asp-action="Delete" asp-route-id="@Model.QuizId">
        <button type="submit" class="btn btn-danger btn-lg">Yes, Delete</button>
        <a asp-action="List" class="btn btn-secondary btn-lg">Cancel</a>
    </form>
</div>

<style>
    h2 {
        font-size: 2rem;
        font-weight: bold;
        margin-bottom: 20px;
    }

    .text-primary {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 10px;
    }

    p {
        font-size: 1.1rem;
        margin-bottom: 10px;
    }

    .btn-lg {
        padding: 12px 30px;
        font-size: 1rem;
    }

    .btn-danger {
        background-color: #dc3545;
        border: none;
        color: white;
    }

    .btn-secondary {
        background-color: #6c757d;
        border: none;
        color: white;
    }
</style>
